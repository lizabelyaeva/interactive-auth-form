# interactive-auth-form
Интерактивная форма авторизации с валидацией на трёх технологиях: нативный JS, сборка с Webpack и React.

# Интерактивная форма авторизации

Проект включает 3 реализации формы авторизации:
1. Нативная версия 
2. Версия с инфраструктурой 
3. Версия на фреймворке 

# 1. Запуск нативной версии

1. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/lizabelyaeva/interactive-auth-form.git
2. Перейдите в папку проекта:
    ```bash
    cd interactive-auth-form/native
3. Откройте файл index.html в браузере

Если ввести следующие данные в форму и нажать «Войти»:

Email: user@example.com

Пароль: securepass123

...будет запущена анимация конфетти 🎉 в виде пасхалки.

## Краткое описание
Проект реализует интерактивную форму входа с:
- Валидацией email и пароля
- Сохранением данных через localStorage
- Полной accessibility-поддержкой
- Адаптивным дизайном
- Анимациями и визуальными эффектами

## Ответы на вопросы ❓

### 1. Реализация accessibility
- Использованы **семантические HTML5-теги**: `<header>`, `<main>`, `<footer>`, `<form>`, `<label>`.
- Каждый `input` связан с `label` через `for` и `id`.
- Атрибуты `aria-required="true"` указывают, что поля обязательны.
- Поля снабжены `aria-describedby` — указывают на вспомогательные или ошибочные сообщения.
- Ошибки отображаются в `role="alert"` с `aria-live="assertive"` — скринридер мгновенно сообщает пользователю об ошибке.
- Кнопка показа пароля использует `aria-pressed`, `aria-controls` и `aria-label` для улучшения понимания её функции.
- `autocomplete` включен для удобства ввода.

Это делает форму более удобной для пользователей с ограниченными возможностями, использующих клавиатуру или экранные читалки.

### 2. Обоснование выбора верстки
В верстке использован **блочный подход** в сочетании с **Flexbox**, поскольку:

- **Блочный подход** обеспечивает логичную структуру и простую вложенность компонентов (шапка, основное содержимое, подвал).
- **Flexbox** применяется для выравнивания элементов внутри формы:
  - Кнопки соцсетей выровнены в ряд (`display: flex`).
  - Контейнер поля пароля позволяет разместить кнопку «показать» рядом с полем (`flex` в `.password-wrapper`).
  - Адаптация на разных ширинах экрана проще с использованием flex-контейнеров.
- Использование **Grid** пока не требуется, т.к. структура формы линейна.

Такой подход обеспечивает простоту поддержки и гибкость адаптивной верстки без лишнего усложнения.


### 3. Выбор брейкпоинтов

Основной брейкпоинт выбран **480px** по следующим причинам:
- Покрывает большинство мобильных устройств (ширина iPhone в портрете - 375px)
- Обеспечивает комфортное отображение формы:
   - Поля ввода не становятся слишком узкими
   - Текст остается читаемым
   - Кнопки сохраняют удобный для нажатия размер
- Оптимален для переключения на одноколоночную верстку
- Учитывает стандартные размеры viewport современных смартфонов

### 4. Почему выбран тот или иной способ хранения данных

- Для хранения email использован `localStorage`:  
    ```js  
    localStorage.setItem('savedEmail', email);
- localStorage сохраняет данные даже после закрытия браузера.
- Это позволяет автоматически подставить email при повторном входе — повышает удобство пользователя.
- Пароль не сохраняется — хранить его в браузере небезопасно (уязвимость к XSS-атакам, риски утечки).
- Такой подход — компромисс между удобством и безопасностью.

### 5. Какие функции больше понравились: стрелочные или именованные
В коде использованы оба типа:
- Стрелочная функция — краткая, удобна для событий:

    ```js
    togglePasswordBtn.addEventListener('click', () => { ... });
- Именованные функции — для повторного использования и лучшей читаемости:
    ```js
    function runConfetti() { ... }


# 2. Подключение инфраструктуры

## Менеджер пакетов

Для управления зависимостями и скриптами проекта выбран менеджер пакетов **npm**.

**Обоснование выбора:**

- npm является стандартным менеджером пакетов для Node.js и предустановлен вместе с ним, что упрощает настройку.
- Обширная поддержка и стабильность.
- Обеспечивает детерминированность версий через lock-файл `package-lock.json`.
- Широкая совместимость с CI/CD и инструментами разработки.

Для инициализации проекта использована команда:
```bash
npm init
```
В package.json в секции engines зафиксированы версии node и npm для обеспечения единой среды разработки:

```bash
"engines": {
  "node": "22.15.0",
  "npm": ">=9.0.0"
}
```
## Фиксация версии Node.js с помощью nvm

Для фиксации версии Node.js используется nvm (Node Version Manager).

**Обоснование:**
- Удобство переключения между версиями Node.js
- Обеспечивает воспроизводимость окружения для всех участников проекта
- Используется как в локальной разработке, так и в CI/CD

**Реализация:**
1. Создан файл `.nvmrc` с указанием версии:
22.15.0

text
2. Версия дополнительно указана в `package.json` в секции `engines`:
```json
"engines": {
  "node": "22.15.0"
}
```
## Подключение TypeScript

Добавлен TypeScript для статической типизации и повышения надежности кода.

### Конфигурация `tsconfig.json`:

- **`"strict": true`**  
  Максимальная строгость проверки типов для раннего обнаружения ошибок

- **`"target": "es6"` и `"module": "esnext"`**  
  Современные стандарты JavaScript для совместимости с новыми инструментами

- **`"outDir": "./dist"`**  
  Скомпилированные файлы выводятся в папку `dist/`

- **`"esModuleInterop": true`**  
  Упрощает взаимодействие с модулями CommonJS

### Пример конфигурации:

```json
{
  "compilerOptions": {
    "target": "es6",
    "module": "esnext",
    "outDir": "./dist",
    "strict": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "moduleResolution": "node",
    "types": ["vite/client", "node", "vitest"],
    "resolveJsonModule": true
  },
  "include": ["src", "vite.config.ts"]      
}
```
## Подключение сборщика

В проект интегрирован сборщик **Vite**.

### Обоснование выбора:

- **Молниеносный дев-сервер**  
  Благодаря использованию нативных ES-модулей обеспечивает мгновенную перезагрузку модулей (HMR)

- **Полная поддержка TypeScript**  
  Встроенная обработка TypeScript без дополнительных плагинов

- **Простая и быстрая настройка**  
  Минимальная конфигурация для начала работы

- **Универсальность**  
  Подходит как для разработки, так и для production-сборки

### Добавлены скрипты в package.json:
```bash
   "scripts": {
  "dev": "vite",
  "build": "vite build"}
```
## Подключение линтер

В проекте используется **ESLint** с плагинами для TypeScript.

### Обоснование выбора:

- **Стандарт индустрии**  
  Наиболее распространённый инструмент для анализа качества кода

- **Богатая экосистема**  
  Поддержка множества плагинов и конфигураций

- **Автоматизация**  
  Возможность автоматического исправления большинства ошибок

### Конфигурация:

Основные используемые правила:
- Базовый набор `eslint:recommended`
- Плагин `@typescript-eslint` для TypeScript
- Правила Airbnb JavaScript Style Guide

Скрипты в package.json:
```json
"scripts": {
  "lint": "eslint . --ext .ts,.tsx",
  "lint:fix": "eslint . --ext .ts,.tsx --fix"
}
```
## Подключение форматтера

В проекте используется **Prettier** для автоматического форматирования кода.

### Обоснование выбора:

- **Автоматизация форматирования** 

- **Устранение разногласий**  

- **Простая интеграция**  

### Конфигурация:

Используется стандартная конфигурация Prettier с небольшими изменениями:

```json
"scripts": {
  "format": "prettier --write ."
}
```
## Подключение фреймворка тестирования 

В проекте используется **Vitest** в качестве основного тестового фреймворка.

### Обоснование выбора:

- Интегрируется с Vite, что упрощает настройку.
- Высокая скорость запуска тестов.
- Поддержка всех необходимых возможностей для юнит-тестов (mock, spy, snapshot).
- Совместимость с Jest-стилем тестов.
### Конфигурация:

Основной конфигурационный файл (`vitest.config.ts`):

```typescript
import { defineConfig } from 'vite';

export default defineConfig({
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: './src/test/setup.ts'
  }
});
```
# 3. Используем фреймворк 

## Общие сведения
Был взят предыдущий проект с инфраструктурой и полностью перенесён на React. Для наглядности и сохранения истории изменений предыдущие версии проекта (нативный и с инфраструктурой) были размещены в отдельных папках.

---

## Подключение React и инфраструктуры 
- Установлена актуальная версия React и React DOM.
- Настроена инфраструктура с использованием TypeScript, ESLint и Vite для быстрой сборки и удобного запуска проекта.
- Выбран сборщик Vite за высокую скорость работы и хорошую интеграцию с React и TypeScript.
- Линтер настроен для соблюдения единых стандартов кода и предотвращения ошибок.

---

## Подключение стейт-менеджера 
- В проект интегрирован Context API для управления состоянием.
- Было принято решение не использовать сторонние стейт-менеджеры, поскольку локального состояния React и Context API оказалось достаточно.
- Context API используется для хранения и управления состоянием формы авторизации и пользовательских данных.

---

## Реализация интерфейса
- Интерфейс был разбит на переиспользуемые функциональные компоненты: форма авторизации, поля ввода, кнопки, сообщения об ошибках.
- Для логики и управления состоянием использовались React-хуки.
- Стилизация компонентов выполнена с помощью CSS-модулей.
- Интерфейс адаптивен и семантически корректен.

---

## Тестирование компонентов 
- Для тестирования компонентов была выбрана библиотека Vitest за хорошую интеграцию с Vite и простоту написания тестов.
- Основные компоненты покрыты тестами, которые проверяют корректность рендеринга и работу валидации формы.
- Тесты запускаются через npm-скрипт `npm test` и обеспечивают стабильность работы приложения при изменениях.

---

В итоге проект был успешно перенесён на React с современным стеком технологий, реализовано управление состоянием, модульность компонентов, покрытие тестами и расширена функциональность за счёт дополнительных возможностей.
